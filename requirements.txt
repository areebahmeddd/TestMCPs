# Runtime dependencies for SuperBox TestMCPs
# These are installed in the Cloudflare Durable Object via micropip (pure-Python PyPI wheels).
# C-extension packages are NOT supported in the Pyodide sandbox.

requests>=2.32.0
